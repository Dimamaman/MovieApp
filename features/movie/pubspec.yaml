name: feature_movie
description: Movie features (screens + routes) for MovieApp
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../../core/core
  common:
    path: ../../common/common
  feature_detail:
    path: ../detail

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

